# Chrome-Extension-URL-Tracker
a simple chrome extension using HTML, CSS and Javascript using chrome API

- Save current active tab in local storage
- Save any ulr in local storage
- Clear all stored URLs
